import { colors } from "./colors";

export const theme = {
    line: {
        theme: colors.themeColor,
    },
    tag_text: {

    },
    gauge_background: {
        light_gray: colors.themeGray,
    },
    gauge_foreground: {
        theme: colors.themeColor,
    }
}

